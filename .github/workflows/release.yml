name: Build emuBro Packages

on:
  push:
    tags:
      - "v*"
      - "*-alpha*"
  workflow_dispatch:

concurrency:
  group: tauri-release-${{ github.ref }}
  cancel-in-progress: false

env:
  NODE_VERSION: 20

jobs:
  bundle:
    name: Bundle (${{ matrix.name }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: windows
            os: windows-latest
            bundles: "nsis,msi"
          - name: linux
            os: ubuntu-latest
            bundles: "appimage,deb"
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf
          if apt-cache show libwebkit2gtk-4.1-dev >/dev/null 2>&1; then
            sudo apt-get install -y libwebkit2gtk-4.1-dev libjavascriptcoregtk-4.1-dev
          else
            sudo apt-get install -y libwebkit2gtk-4.0-dev libjavascriptcoregtk-4.0-dev
          fi

      - name: Sync emuBro resources
        run: npm run sync:resources

      - name: Install root JS dependencies
        timeout-minutes: 20
        run: npm install --no-audit --prefer-offline --no-fund

      - name: Install Tauri JS dependencies (Windows)
        if: runner.os == 'Windows'
        timeout-minutes: 20
        run: npm --prefix tauri install --no-audit --prefer-offline --include=optional --no-fund

      - name: Install Tauri JS dependencies (non-Windows)
        if: runner.os != 'Windows'
        timeout-minutes: 20
        run: npm --prefix tauri install --no-audit --prefer-offline --include=optional --no-fund

      - name: Build legacy renderer bundle
        run: npm run legacy:build

      - name: Build Tauri bundles
        run: npm --prefix tauri run tauri:build -- --bundles ${{ matrix.bundles }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: emubro-${{ matrix.name }}-bundles
          path: |
            tauri/src-tauri/target/release/bundle/**

  flatpak:
    name: Flatpak (linux)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable

      - name: Install Flatpak tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y flathub org.freedesktop.Platform//24.08 org.freedesktop.Sdk//24.08

      - name: Sync emuBro resources
        run: npm run sync:resources

      - name: Install root JS dependencies
        timeout-minutes: 20
        run: npm install --no-audit --prefer-offline --no-fund

      - name: Install Tauri JS dependencies (Linux)
        timeout-minutes: 20
        run: npm --prefix tauri install --no-audit --prefer-offline --include=optional --no-fund

      - name: Build legacy renderer bundle
        run: npm run legacy:build

      - name: Build Flatpak bundle
        run: npm run build:linux:flatpak

      - name: Upload Flatpak artifact
        uses: actions/upload-artifact@v4
        with:
          name: emubro-flatpak
          path: build/flatpak/emuBro.flatpak

  publish-release:
    name: Publish GitHub Release Assets
    if: startsWith(github.ref, 'refs/tags/') && always()
    needs:
      - bundle
      - flatpak
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: emubro-*
          path: release-assets
          merge-multiple: true

      - name: Show release files
        run: |
          ls -la release-assets
          find release-assets -type f | sort

      - name: Upload assets to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            release-assets/**
